<% provide(:title, @user.name)%>
<div>
  <aside>
    <section>
      <h1>
        <%= gravatar_for @user, size: 135 %>
        <%= @user.name %><br/><br>
      </h1>	
	<strong><%= @total_questions.count %> Questions</strong>
	<strong style="padding-left:7em"><%= @total_answers.count %> Answers</strong>
	<strong style="padding-left:7em"><%= @total_comments.count %> Comments</strong>
      
    </section>
    <div class="tabbable">
      <ul class="nav nav-tabs ">
        <li class="active  offset2"><a href="#tab1" data-toggle="tab">Profile</a></li>
        <li><a href="#tab2" data-toggle="tab">Questions</a></li>
        <li><a href="#tab3" data-toggle="tab">Answers</a></li>
        <li><a href="#tab4" data-toggle="tab">Comments</a></li>
	<li><a href="#tab5" data-toggle="tab">About me</a></li>
      </ul>
      <div class="tab-content">
      <div class="tab-pane active" id="tab1">
        <%= render partial: 'profile' %>
      </div>
      <div class="tab-pane" id="tab2">
        <%= render partial: 'question' %>
      </div>
      <div class="tab-pane" id="tab3">
        <% @answers.each do |answer| %>
	  <div class="row">
	    <div class="span10">
	      <li><%= link_to answer.question.title, question_path(@question) %></li>
	    </div>
	  </div><br>
	<% end %>
      </div>
      <div class="tab-pane" id="tab4">
	<% @comments.each do |comment| %>
	  <div class="row">
	    <div class="span10">
	      <li><%= link_to comment.question.title, question_path(@comment) %></li>
	    </div>
	  </div><br>
	<% end %>
      </div>
      <div class="tab-pane" id="tab5">
        <%= @user.aboutme %>
      </div>
  </div>
</div>
  </aside>
</div>
